# {{PROJECT_NAME}} — Coding Rules Pointer
#
# This file is read by Windsurf AI.
# It is a POINTER, not a copy — do not add rules here.
# All rules (architecture, coding rules, prohibited items, validation,
# steering loop) live in AGENTS.md at the repository root. Read it before
# making changes, and edit rules only there.

Quick reference (mirrors AGENTS.md headings — content lives there):
- Architecture: domain <- application <- infrastructure <- presentation
- Validation: ./scripts/validate.sh
- Workflow prompts: .claude/commands/*.md (start, plan, done, fix, commit, review, test, adr, coverage)
