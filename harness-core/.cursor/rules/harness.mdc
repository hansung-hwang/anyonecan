---
description: Harness Engineering Framework — coding rules and architecture guide
alwaysApply: true
---

# {{PROJECT_NAME}} — Rules Pointer

This file is a **pointer, not a copy**. All project rules (architecture,
coding rules, prohibited items, validation, steering loop) live in
`AGENTS.md` at the repository root — the single source of truth shared by
every AI tool. Read it before making changes, and edit rules only there.

Quick reference (mirrors `AGENTS.md` headings — content lives there):

- Architecture: `domain` ← `application` ← `infrastructure` ← `presentation`
- Validation: `./scripts/validate.sh`
- Workflow prompts: `.claude/commands/*.md` (start, plan, done, fix, commit, review, test, adr, coverage)
