# anyonecan (Harness Engineering Framework) — Coding Rules Pointer
#
# ⚠ This file is for framework development only.
# To create a new project, run setup.ps1 (Windows) or ./setup.sh (Mac/Linux),
# then run the AI tool from the generated project directory.
#
# This file is read by Windsurf AI. It is a POINTER, not a copy — do not add
# rules here. All rules (architecture, coding rules, prohibited items,
# validation, steering loop) live in AGENTS.md at the repository root.
# Read it before making changes, and edit rules only there.

Quick reference (mirrors AGENTS.md headings — content lives there):
- Architecture: domain <- application <- infrastructure <- presentation
- Validation: pnpm validate
- Workflow prompts: .claude/commands/*.md (start, plan, done, fix, commit, review, test, adr, coverage)
