# WORKLOG

Append-only log of completed work sessions. Run `/done` at the end of each
session to add an entry — never edit or delete past rows.

## Format

| Date | Summary | Files Changed | Plan |
|------|---------|----------------|------|
| YYYY-MM-DD | What was done | key files/dirs touched | `.workspace/plans/xxx.md` (or —) |
| 2026-07-13 | Added `.workspace/` work journal + `/plan` `/done` commands (harness-core + root). P1 done: AGENTS.md is now the single rule source; CLAUDE.md/.cursorrules/.windsurfrules/harness.mdc reduced to pointers; setup scripts narrowed; `scripts/check-sync.mjs` added as a drift regression guard. Fixed a `setup.sh` perl multi-`-e` bug found during verification. Committed as f34eb77. | `.workspace/`, `.claude/commands/`, `AGENTS.md`, `CLAUDE.md`, `.cursorrules`, `.windsurfrules`, `harness-core/*` (same set), `setup.ps1`, `setup.sh`, `scripts/check-sync.mjs`, `scripts/validate.sh`, `package.json`, `README.md` | `.workspace/plans/2026-07-13-harness-upgrade-p1-p5.md` |
| 2026-07-13 | P2 done: added `HARNESS-VERSION` + `harness-manifest.json` + `upgrade.ps1`/`upgrade.sh`/`upgrade.py` so existing generated projects can pull in framework updates. `setup.ps1`/`setup.sh` now write `.harness-meta.json`. Verified end-to-end (generate → simulate staleness → upgrade → diff) on both Windows and Mac/Linux paths; found and fixed 3 real bugs during verification: ADR-001's `{{DATE}}` placeholder regression, language-pack-overlaid files (settings.json/ci.yml/husky) being silently downgraded when misclassified as language-agnostic, and CRLF/LF checkout noise causing spurious "changed" reports. Documented the version-bump rule in `AGENTS.md` + `README.md`, added `FRAMEWORK-CHANGELOG.md`. | `harness-core/HARNESS-VERSION`, `harness-core/harness-manifest.json`, `upgrade.ps1`, `upgrade.py`, `upgrade.sh`, `setup.ps1`, `setup.sh`, `AGENTS.md`, `README.md`, `FRAMEWORK-CHANGELOG.md` | `.workspace/plans/2026-07-13-harness-upgrade-p1-p5.md` |
| 2026-07-13 | P3 done: promoted documented-only rules to hard gates across all 3 languages. TS: added `.only()` ban + explicit `ban-ts-comment` override (recommended eslint default was softer than AGENTS.md's outright ban), narrowed vitest coverage to `src/domain/**`, added CI coverage step. Python: enabled ruff `T20` (verified live with real ruff — flags `print()`), added `--cov-fail-under=80` scoped to `src/domain` to CI (verified live with real pytest+pytest-cov: fails at 67%, passes at 100%). Java: added Checkstyle rule for `System.out.print*`, added a `coverage` Maven profile (JaCoCo, project-wide not domain-scoped — documented why), and brought the arch test up to the same 5-check parity as TS (added domain purity, layer-cycle, and domain→test-exists checks) — Python was already at full parity. Simplified `/commit`'s pre-scan to nothing (linter now covers everything it used to grep for). Java/TS changes could only be syntax/well-formedness checked, not compiled or run (no Maven/Java, no node_modules in this session) — flagged as a follow-up for the user to confirm with `mvn verify -P coverage` / `pnpm validate` once available. | `eslint.config.js` (root + TS pack), `vitest.config.ts` (root + TS pack), both `.github/workflows/ci.yml` for TS/Python/Java, `language-packs/python/pyproject.toml`, `language-packs/java/checkstyle.xml`, `language-packs/java/pom.xml`, `language-packs/java/src/test/java/arch/DependencyTest.java`, `.claude/commands/commit.md` (both copies), `harness-core/.claude/commands/coverage.md`, `README.md` | `.workspace/plans/2026-07-13-harness-upgrade-p1-p5.md` |
| 2026-07-13 | Session close (`/done`): P1, P2, P3 of the harness-upgrade plan are done and committed (f34eb77, cf39a92, 834a40b). User is stopping for the day; P4 (team collaboration layer: PR template, move how-to docs into harness-core, multi-member `.workspace` note, named CI steps) and P5 (data-driven language packs via pack.json) remain, to be picked up in a future session. No uncommitted changes. | — (session boundary only, no new file changes) | `.workspace/plans/2026-07-13-harness-upgrade-p1-p5.md` |
| 2026-07-14 | P4 + P5 done, completing the harness-upgrade plan. P4: added `harness-core/.github/PULL_REQUEST_TEMPLATE.md` (also mirrored at root); moved `docs/how-to/git-workflow.md`/`testing-guide.md` into `harness-core/docs/how-to/` generalized for all 3 languages (previously these existed only in this repo's own docs and were never copied into generated projects — a real gap); documented multi-member `.workspace/` conflict handling in `plans/README.md` + AGENTS.md Work Journal (both copies); split CI's single "Validate" step into named Typecheck/Lint/Test steps for TS and Python (Java's `mvn verify` left as one step). P5: added `pack.json` per language (display, order, default, aliases, rules, banned, postGenerate, install.candidates); rewrote `setup.ps1` and `setup.sh` to discover language packs by globbing `language-packs/*/pack.json` instead of hardcoded 3-way switches — adding a language no longer requires touching the setup scripts; added `docs/how-to/adding-a-language-pack.md` (full contract) and rewrote README's language-pack section to match. Verified P5 end-to-end on this Windows machine: generated one project per language via `setup.ps1` with piped stdin (TS/Python/Java, including Java's base-package prompt), plus alias-based input (`python`) and blank-input default. `setup.sh`'s rewrite was only `bash -n` syntax-checked, not run end-to-end (no Mac/Linux available). HARNESS-VERSION bumped 1.0.0 → 1.2.0 (single release covering both phases, per user's request to hold the commit until P5 was done); `FRAMEWORK-CHANGELOG.md` entry added. Plan marked Done. | `harness-core/.github/PULL_REQUEST_TEMPLATE.md`, `.github/PULL_REQUEST_TEMPLATE.md`, `harness-core/docs/how-to/{git-workflow,testing-guide}.md`, `docs/how-to/adding-a-language-pack.md`, `.workspace/plans/README.md` (both copies), `AGENTS.md` (both copies), `.github/workflows/ci.yml` (root + TS + Python packs), `language-packs/*/pack.json`, `setup.ps1`, `setup.sh`, `harness-core/harness-manifest.json`, `harness-core/HARNESS-VERSION`, `FRAMEWORK-CHANGELOG.md`, `README.md` | `.workspace/plans/2026-07-13-harness-upgrade-p1-p5.md` |
| 2026-07-24 | Multi-agent coordination (Harness 1.4.0) plan done, all six gates (M0-M6) passed. Design audited against real enforcement (check-sync.mjs, harness-manifest.json, the three upgrade scripts), then deliberately scoped down after a complexity-budget review: shipped the guide, an always-on AGENTS "Handoff and Reporting" section, `/coordinate`, and `/plan`'s optional Parallelization block; deferred `/start`/`/commit`/`/review`/`/done` prompt edits and a mechanical scope checker to 1.5.0 so solo users see zero overhead. Added a light multi-human collaboration layer (PR review as the Coordinator's integration gate) alongside it. M1 prototyped in root `AGENTS.md`/`docs/how-to/` first, found and fixed two real bugs during self-review (wrong worktree-path depth, stale cross-references) before M2 generalized it into `harness-core/` (new guide + `/coordinate` both copies + AGENTS/CLAUDE pointers + manifest registration + a new check-sync manifest-registration guard). M3 bumped `HARNESS-VERSION` to 1.4.0 and updated `FRAMEWORK-CHANGELOG.md`/`README.md`. M4 generated real TypeScript/Python/Java projects via `setup.ps1` (non-interactive, piped stdin) and confirmed identical, uncorrupted delivery. M5 upgraded a real disposable 1.3.0 project (checked out via a temporary `git worktree` at the pre-session commit) to 1.4.0 and confirmed the full customization-safety contract holds end-to-end; found and fixed an unrelated pre-existing bug in the process (`upgrade.ps1`/`upgrade.py` never updated `.harness-meta.json`'s own `harnessVersion` field). One caveat carried forward: `pnpm`/`uv`/`mvn` are unavailable in this session's environment (Node 18.17, no admin rights, `corepack enable` failed), so the full `pnpm validate`/`validate.sh` suites were never actually run end-to-end — `node scripts/check-sync.mjs` (the exact first step) passed after every edit instead. Separately, designed a follow-on team-roles/project-mode feature (Solo/Team mode at setup, changeable via a new `/team` command, per-role file-scope ownership mapped onto the existing clean-architecture layers) as its own plan, provisional 1.5.0; Gate T0 closed (command name, 7-role catalog, active-role detection, upgrade-safe metadata storage, prose-only enforcement) but implementation not started, deliberately sequenced after this plan. Seven commits: 787e7b3, 240bc9a, 5e766f5, 7b36860, 527499b, 707a81a, 6af3106. | `AGENTS.md`/`CLAUDE.md` (root + `harness-core`), `harness-core/docs/how-to/multi-agent-collaboration.md`, `docs/how-to/multi-agent-collaboration.md`, `.claude/commands/coordinate.md` (root + `harness-core`), `harness-core/.claude/commands/plan.md`, `harness-core/.workspace/plans/README.md`, `harness-core/harness-manifest.json`, `harness-core/HARNESS-VERSION`, `scripts/check-sync.mjs`, `FRAMEWORK-CHANGELOG.md`, `README.md`, `upgrade.ps1`, `upgrade.py` | `.workspace/plans/2026-07-22-multi-agent-coordination.md` (Done); companion design `.workspace/plans/2026-07-23-team-roles-and-project-mode.md` (In Progress, Gate T0 closed) |
| 2026-07-24 | Post-close-out audit of the 1.4.0 plan: checked every in-scope item against the actual files (not just re-reading the plan) — all 14 in-scope artifacts confirmed present and correct, all 4 deferred prompts confirmed untouched (zero `git diff` since the 1.3.0 commit), `check-agent-scope.*` confirmed absent. Found one real discrepancy: the root (framework-dev) copy of the guide still had its M1-prototype §13 title ("Multiple humans...") while the harness-core copy (the one that ships) already matched the plan's spec ("Working as a team..."). Fixed root to match. Pushed all 9 pending commits (`787e7b3`..`671c13b`) to `origin/main`. No other work this stretch — remaining item (full `pnpm validate` in a toolchain-complete environment) explicitly deferred to next session per user. | `docs/how-to/multi-agent-collaboration.md` | `.workspace/plans/2026-07-22-multi-agent-coordination.md` (Done) |
| 2026-07-25 | Closed the 1.4.0 validation caveat: got working toolchains in this Node-18.17/no-admin environment (pnpm 10 — the only pnpm major compatible with both Node 18 and this repo's `pnpm-workspace.yaml` `allowBuilds` key — installed to a user-writable npm `--prefix`; `uv` via `pip install --user`), then ran root `pnpm validate` (typecheck/lint/test all pass) and, per language, generated a fresh project via `setup.ps1` and ran its own `validate.sh` end-to-end: TypeScript passed cleanly; Python failed on the first attempt ("No module named mypy") despite `setup.ps1` reporting "uv sync complete". Root-caused to two real bugs in `setup.ps1`'s (and `setup.sh`'s) "Installing dependencies" step: (1) the success message was printed unconditionally, never checking the install command's actual exit code; (2) `setup.ps1` additionally merged native stderr into the error stream via `2>&1` under a script-wide `$ErrorActionPreference = "Stop"`, so a routine informational stderr line from `uv` was promoted to a terminating exception that aborted the install block mid-run while the child process kept installing in the background — the exact PowerShell 5.1 footgun this harness's own tool guidance warns about. Fixed both scripts to only report success on a real zero exit code; scoped `setup.ps1`'s `$ErrorActionPreference` to `Continue` around the install block (save/restore via `finally`). Re-verified: regenerated fresh TS and Python projects post-fix, both `validate.sh` runs pass end-to-end; confirmed no regression on the TS/pnpm path. Java/Maven remain unavailable — `winget` requires interactively accepting Microsoft Store terms before resolving *any* package (even non-store ones) and that prompt can't be satisfied non-interactively; did not force it by touching system-level winget source config. `HARNESS-CHANGELOG.md` entry added. | `setup.ps1`, `setup.sh`, `HARNESS-CHANGELOG.md` | — |
| 2026-07-25 | Committed the above fix (`f3b9e34`) plus its workspace docs (`52820ab`) at user's request. Then started Harness 1.5.0's team-roles/project-mode plan, Phase T1 (config model & setup) — Gate T0 was already closed from a prior session. Added a Solo/Team prompt to both `setup.ps1` and `setup.sh` (numbered menu, defaults to Solo, matching the existing language/comment-language prompt style); `.harness-meta.json` now records `projectMode`, with Solo omitting `roles`/`roster` entirely and Team writing empty `[]`/`{}` for a future `/team` command to populate; added a `{{TEAM_ROLES_SECTION}}` token to `harness-core/AGENTS.md` (between "Handoff and Reporting" and "Key Invariants") that setup strips to nothing for Solo (litmus: zero visible diff from before) or expands to a `## Team & Roles` placeholder section for Team. Verified end-to-end with real generations, not just review: ran both `setup.ps1` and `setup.sh` (the latter smoke-tested directly under Git Bash, which happens to be present on this Windows box, in addition to the earlier syntax-only check) for Solo × Team across TypeScript and Python; confirmed the AGENTS.md diff between a Solo and Team generation is exactly the new section; each generated project's `validate.sh` still passes in every combination; `node scripts/check-sync.mjs` still passes. Plan file updated with Gate T1 closed. Paused here, uncommitted, as a checkpoint before Phase T2 (`/team` command — a new command file, warranting more explicit review than T1's config-model change) per the user's own preference for reviewing before committing. | `setup.ps1`, `setup.sh`, `harness-core/AGENTS.md` | `.workspace/plans/2026-07-23-team-roles-and-project-mode.md` (In Progress, Gate T1 closed) |
| 2026-07-25 | Committed T1 (`2044a28`) at user's request, then did Phase T2 of the 1.5.0 plan: the `/team` command itself. Wrote `harness-core/.claude/commands/team.md` (steps: read current state, ask init/switch/edit, handle Solo↔Team switching, present the default 7-role catalog, write the roster + trimmed ownership table + in-role convention into `AGENTS.md`'s `Team & Roles` section, mirror to `.harness-meta.json`) and a root copy adapted for this repo's own situation — no `Key Invariants` section to anchor placement against (uses `Framework Versioning` instead), no `.harness-meta.json` yet (documented the bootstrap case), and a note distinguishing `/team` (standing ownership) from `/coordinate` (one task's split), mirroring how `coordinate.md`'s root/harness-core copies already diverge for repo-specific concreteness. Registered the new command in both AGENTS Workflow Prompts tables, both CLAUDE.md command lists, and `harness-manifest.json`'s `frameworkOwned` (1.4.0's findings A/B/E, all still relevant). Since `/team` is a prompt file with no separate script to unit-test, verified via: `check-sync.mjs` passing with the new command in both copies + the manifest; a fresh Team-mode TypeScript generation actually delivering `team.md` and picking it up in its own baseline-hash map (confirms the manifest-registration guard's live effect, not just the static check); and a manual dry run hand-applying exactly the command's Steps 5-6 (a 2-role roster into the scaffolded AGENTS.md section, `roles`/`roster` into `.harness-meta.json`) which produced valid JSON and a passing `validate.sh`. No live agent actually invoked `/team` — same content-review-plus-dry-run precedent as `/coordinate`'s M4 verification. Plan file updated with Gate T2 closed. Paused again, uncommitted, before Phase T3 (documenting the in-role convention in the multi-agent guide + a role-scoping dry run), continuing the same per-phase checkpoint rhythm the user set at T1. | `.claude/commands/team.md`, `harness-core/.claude/commands/team.md`, `AGENTS.md`, `CLAUDE.md`, `harness-core/AGENTS.md`, `harness-core/CLAUDE.md`, `harness-core/harness-manifest.json` | `.workspace/plans/2026-07-23-team-roles-and-project-mode.md` (In Progress, Gate T2 closed) |
| 2026-07-25 | Committed T2 (`9c1126c`) at user's request, then did Phase T3 of the 1.5.0 plan: role-scoped agent behavior. Extended `docs/how-to/multi-agent-collaboration.md` §13 "Working as a team" (both root and harness-core copies) — that section had explicitly deferred "Team roles" to this exact feature during 1.4.0; replaced the deferral note with the actual in-role convention (active role by declaration/branch-prefix, edits restricted to owned scope, cross-role changes escalated as a request note) and a pointer to `/team`. Then ran two real dry runs (not simulation) to satisfy the phase's second acceptance item: launched a fresh, zero-prior-context `general-purpose` agent against a live Team-mode TypeScript generation with a hand-populated `Team & Roles` section (2-role roster), declared its active role as Backend, and gave it a task requiring edits to `domain` interfaces + `docs/adr/` — both explicitly Architect-owned. Run 1: the agent correctly self-constrained and wrote an escalation note instead of editing directly, citing the right AGENTS.md passages — but its own honest self-report flagged that it had to *infer* where to put the note, since no location was specified; a less careful agent might have just edited the files, since "nothing loudly says STOP at the point of editing." Treated this as a real finding, not a pass/fail verdict, and fixed it in the same session: made the escalation note's location concrete (`.workspace/plans/<date>-<short-topic>-request.md`, addressed to the owning role) in `/team`'s own generation instructions (both copies) and the guide's new paragraph (both copies). Run 2 (fresh agent, different field, same scenario) confirmed the fix — the agent reported the location was given verbatim, not inferred, and escalated correctly to that exact path. Noted two smaller residual ambiguities the second run surfaced (whether "propose to Architect" permits a draft edit vs. note-only; whether a STATUS.md update is mandated by the escalation rule) without chasing them further, consistent with 1.5.0's explicit prose-only enforcement scope. Plan file updated with Gate T3 closed, including the full before/after of the finding. Paused again, uncommitted, before Phase T4 (the plan's last phase: version bump, changelog, README, fresh-gen/upgrade matrices), continuing the same per-phase checkpoint rhythm. | `.claude/commands/team.md`, `harness-core/.claude/commands/team.md`, `docs/how-to/multi-agent-collaboration.md`, `harness-core/docs/how-to/multi-agent-collaboration.md` | `.workspace/plans/2026-07-23-team-roles-and-project-mode.md` (In Progress, Gate T3 closed) |
| 2026-07-25 | Committed T3 (`8b98bbb`) at user's request, then did Phase T4 of the 1.5.0 plan (the last phase): version, docs, matrices. Bumped `harness-core/HARNESS-VERSION` 1.4.0 → 1.5.0, added a `FRAMEWORK-CHANGELOG.md` entry summarizing all four phases, and updated `README.md` (structure tree gains `team.md`, Quick Start's example prompts/commands gain the mode prompt and `/team`, new "Team Roles" section after "Work Journal"). Completed the fresh-generation matrix's remaining language (TS/Python were done at T1): generated Solo and Team Java projects via `setup.ps1`, correcting the prompt order for Java's extra base-package question (mode prompt precedes it, found via a first failed attempt where "team" got consumed as the base-package answer). Confirmed the same Solo/Team `AGENTS.md` diff litmus holds for Java, `.harness-meta.json`/`basePackage` are both correct, and the Java package-directory structure generates normally under both modes; `mvn`/`javac` remain unavailable in this environment (same `winget` ToS-prompt block as before), so Java's `validate.sh` itself is still unverified end-to-end. Then ran a real upgrade matrix via disposable `git worktree` checkouts, mirroring the 1.4.0/M5 technique — not simulation: (A) generated a Team TypeScript project from the pre-T4 commit (`8b98bbb`, already has T1-T3's code), hand-populated real `roles`/`roster` data (not the empty scaffold), upgraded it with the current tree's `upgrade.ps1`, and confirmed `projectMode`/`roles`/`roster` survived byte-for-byte while `harnessVersion` advanced correctly and `git diff --stat` showed only `.harness-meta.json`/`HARNESS-VERSION` changed (`AGENTS.md`/`team.md` untouched); (B) generated a genuinely pre-1.5.0 Solo project from `52820ab` (before any Solo/Team code existed — no mode prompt, no `team.md`), upgraded it, and confirmed `team.md` + the guide were delivered as new/updated files while `.harness-meta.json` gained **zero** `projectMode`/`roles`/`roster` keys — "Solo unaffected" confirmed structurally, not asserted. Both upgraded projects' `validate.sh` still pass; all worktrees/scratch dirs cleaned up. Plan file updated with Gate T4 closed and the plan's own **Status marked Done** — all four implementation phases (T1-T4) plus both design gates (T0, and the four implementation gates) complete. Paused one final time, uncommitted, at the very end of the plan — same per-phase checkpoint rhythm carried through to the finish. | `harness-core/HARNESS-VERSION`, `FRAMEWORK-CHANGELOG.md`, `README.md` | `.workspace/plans/2026-07-23-team-roles-and-project-mode.md` (Done) |
| 2026-07-25 | Committed T4 (`c63bfae`) and the session's workspace close-out (`0bb1e5d`) at user's request, then ran a **post-close-out audit** at the user's explicit request ("계획이 잘 반영됐는지, 보완할 점은 없는지 점검해줘") — checking the actual files against the plan rather than re-reading the plan, same practice as 1.4.0's own post-close-out audit. Found two real gaps. (1) Docs-only: 1.4.0 named an explicit n=2 trigger for un-deferring `/start`/`/commit`/`/review`/`/done` coordination edits, and this plan's own Release Staging said they "can ride here too" — 1.5.0 shipped without them (correctly; n=2 hasn't happened), but nothing recorded that as a decision rather than an omission. Fixed by stating the deferral explicitly in `FRAMEWORK-CHANGELOG.md` and the plan. (2) A real gap, found by actually running `/team` for the first time — every prior verification (T2, T3) had been content review or a *manual* dry run (hand-editing files to simulate the command's output), never a live agent following the command file itself. Generated a fresh Solo project, handed a fresh agent `.claude/commands/team.md` and a switch-to-Team scenario, let it run for real: structurally correct (right insertion point, clean `.harness-meta.json` mutation), but the agent's own report flagged that Step 6 had no worked JSON example, so it inferred kebab-case role ids and a person→role roster direction rather than following a spec. A second live run (edit path: multi-word role + a person with two roles) initially seemed to show the gap still open — but that was a test-setup mistake, not a real failure: the target project had been generated before the fix, so it still had the stale `team.md`; caught via the agent's own diagnostic ("`.claude/commands/team.md` — I did NOT edit this, it was already like this"), corrected by copying the fixed file in, and re-run fresh. That corrected run confirmed multi-word kebab-case derivation now works with zero inference, but surfaced one smaller residual ambiguity (bare array vs. nested-under-a-key for a multi-role person's roster value) — tightened the wording once more rather than spending a fourth live run on it, three real runs being enough evidence for a prose-only 1.5.0 feature. Both `team.md` copies now specify the exact `roles`/`roster` JSON shape with a worked example. Plan file gained a "Post-close-out audit" section recording both findings and fixes in full. This diff is left uncommitted per the user's standing preference this session (review before commit); push remains on hold per explicit request earlier in the session. | `.claude/commands/team.md`, `harness-core/.claude/commands/team.md`, `FRAMEWORK-CHANGELOG.md`, `.workspace/plans/2026-07-23-team-roles-and-project-mode.md` | `.workspace/plans/2026-07-23-team-roles-and-project-mode.md` (Done; post-close-out audit complete) |
| 2026-07-25 | Pushed all 9 accumulated commits to `origin/main` (`9a21371..7695059`) at the user's go-ahead — the 1.5.0 work had been deliberately held unpushed all session. Then analyzed applying the new 1.5.0 release to **Homographormer**, a real sub-project built with this framework (own git repo, Python pack, Korean, currently 1.3.0 — it never received 1.4.0). Did this as a **real dry run, not a paper exercise**: copied the project into scratch at its exact git HEAD with a clean tree, ran the actual `upgrade.py` against the copy, recorded the output, deleted the copy, and verified the real project was untouched (still 1.3.0, clean). Result: the user's hard requirement holds — **zero** source files touched (nothing under `src/`, `HomoGraphormer/`); upgrade only writes manifest-listed paths. But it surfaced a genuine hazard: the project's hand-written 22KB Korean multi-agent guide (custom agent roles, Phase-0 wave assignments) would be **silently overwritten**. Root-caused by reading `upgrade.py:190-201` — a `frameworkOwned` path with no recorded baseline falls back to unconditional overwrite. Investigating *why the project made that mistake* showed **it didn't**: its own `harness-manifest.json` copy (generated at 1.3.0) listed only `git-workflow.md`/`testing-guide.md` under `docs/how-to/`, so the path looked unclaimed — and the manifest isn't `frameworkOwned`, so upgrade never refreshes it and the ownership map silently rots. Wrote three fully cross-linked plans, all design-only, nothing executed: (1) the Homographormer upgrade itself, with a manual rescue phase for the guide; (2) **upgrade tooling** — add `--dry-run` (none exists; previewing required cloning 1.4GB) and stop overwriting project-authored files, with the design nuance that the fix must be surgical rather than a blanket "no baseline → `.new`", since a genuine pre-1.3.0 project must keep the overwrite path or a legitimate migration becomes a `.new` storm; (3) **file-ownership rules** — the user's own proposal, that the framework must *state* which files a project may edit; ships a three-tier rule, a "don't create files inside framework-owned directories" rule naming where they go instead, a check-sync guard against doc/manifest drift, and makes the manifest `frameworkOwned` so it stops going stale. Sequencing stated identically in all three (tooling → file-ownership → Homographormer) because each unblocks the next. Committed as `28691c5`; no code and no external project changed this stretch. | `.workspace/plans/2026-07-25-apply-harness-1.5.0-to-homographormer.md`, `.workspace/plans/2026-07-25-upgrade-tooling-safety-and-dry-run.md`, `.workspace/plans/2026-07-25-file-ownership-rules.md` | all three above (In Progress, design complete) |
